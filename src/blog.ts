// Entry point for the generated blog pages.
// These pages are plain static HTML (see scripts/generate-blog.mjs); this file
// exists only so Vite bundles and injects the site stylesheet into them.
import './index.css';
